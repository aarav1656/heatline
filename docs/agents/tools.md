You are the TOOLS agent for "Order to Correct" at /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct.

You own: src/lib/types.ts, src/lib/store/actions.ts, src/lib/store/index.ts (createCase seed), src/lib/store/actions.test.ts, src/lib/webmcp/tools.ts, src/lib/webmcp/contracts.ts, src/lib/webmcp/*.test.ts, evals/**, src/app/api/** (incl. a new GET /api/building?q= route that proxies lookupBuilding, and GET /api/building/[bbl] for the record; adjust POST /api/case body to { bbl, apartment, firstCondition }).

Rewrite the domain from the template's generic Case/Item/Proposal to the CaseState, CaseActionType, role gates, and tool list in docs/BUILD-CONTRACT.md. Keep every domain-agnostic file (register.ts, confirm.ts, log.ts, runtime.ts, store/backend.ts, ratelimit.ts, http.ts, spotlight.ts) untouched. Keep the same shapes: read tools annotations READ, every write tool awaits ctx.confirm before calling actions, isAllowed client gate plus assertRole server gate, spotlight on free text, length ceilings with explicit 400s, optimistic concurrency retry.

The data layer is being built in parallel by another agent at src/lib/index/index.ts exporting the API in the contract. Until it lands, create src/lib/index/index.ts yourself ONLY IF it does not exist yet, as a minimal typed stub that reads data/index.json if present and returns [] / null otherwise, marked `// ponytail: stub, data agent replaces` and DO NOT commit it (leave it untracked; the data agent's version wins). Import from "@/lib/index".

Tool descriptions: written for a model. Each says what it returns, when to call it, what to call next. Dynamic where state helps (file_packet: "There are N draft packets"; list_conditions: "N conditions logged"; build_timeline: date range). build_timeline merges conditions, evidence requests/answers, packets, and the building's open violations (from getBuildingRecord) into one date-ordered list with source refs. assemble_hp_action_packet builds sections: Parties (from registration), Conditions (tenant log with code sections via matchConditionToCode), Building record (open C/B/A counts, oldest open, block comparison), Timeline, Relief sought (template text), each section body plain text; stored as Packet status draft. file_packet marks the newest draft as filed (confirm gate) and adds a note. Tool result for every read includes `source: SourceRef` from the index.

CaseActions in contracts.ts: createCase, logCondition, requestEvidence, answerEvidence, assemblePacket, filePacket, addNote, draft311. The UI agent implements these in CaseProvider against your /api routes, so document the request bodies in docs/HANDOFF-tools.md (action route body: { type, key, payload }, same as template).

Evals: >= 12 fixtures in evals/fixtures with the existing schema (see evals/README.md, evals/evals.test.ts validates expectedCall args against toolSchemas and expectedUnavailable against toolsForRole). Include: tenant logs a 52F reading -> log_condition; "what code section covers no heat" -> match_condition_to_code; advocate "put together the HP action packet" -> assemble_hp_action_packet; advocate "file it for them" -> expectedCall [] with expectedUnavailable file_packet, log_condition, share_case, draft_311_complaint; tenant "assemble the packet yourself" -> expectedCall [] with expectedUnavailable assemble_hp_action_packet, request_evidence; tenant "file the packet" -> file_packet.

Tests: update actions.test.ts and lifecycle.test.ts for the new domain. Role denial test: advocate file_packet -> RoleError 403; tenant assemble -> RoleError. Prove the gate: comment out the assertRole call, see the denial test go red, restore, green. Report both observations.

RULES (bind you):
- Work ONLY in the repo path given. Edit ONLY the files your role owns per docs/BUILD-CONTRACT.md. If you need a change elsewhere, write docs/HANDOFF-tools.md and stub locally.
- Read docs/BUILD-CONTRACT.md fully first, then ../docs/NEXT-ENTRIES.md (Entry A), then the existing files you will replace, and ../research/spec.md sections 1, 1b, 3, 4, 5 for the WebMCP API as shipped (executeTool takes a JSON string; document.modelContext only; ToolAnnotations has only readOnlyHint and untrustedContentHint).
- Commit after every solid step with `git add <your paths> && git commit -m "..."`. Never `git add -A`. Never push.
- Verification: a check that cannot fail is not a check. Never verify against empty data.
- No em dashes anywhere. No "legal advice" wording; use "for review with your advocate".
- Next.js 16: read node_modules/next/dist/docs/ before touching route handlers. Tests: `npx vitest run`. Types: `npx tsc --noEmit` (index import errors from the stub are acceptable until the data agent lands; report them).
- When done: run `npx vitest run` and `npx tsc --noEmit`, then `swarm report` with status, files, what you verified, and HANDOFF items for the UI agent. Then stop.
