You are the REDESIGN agent for "Order to Correct" at /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct.

Why: the current look is a near-copy of the first entry (Out of Service: warm paper canvas, Archivo, MTA-signage panels). Judges will see three entries from this family and they must not look like the same product. Give this one its own visual identity.

Reference system: docs/DESIGN-SOURCE-wired.md (Wired: white canvas, black ink, tall condensed serif display, hairline rules, editorial column rhythm, one link blue #057dbc). Read it once, fully. Adapt, do not copy branding: no Wired wordmark, no proprietary fonts (use the fallback stack: display = a condensed serif via next/font from Google, e.g. "Bodoni Moda" or "Playfair Display" at 700 with tight tracking; body = "Inter" or "IBM Plex Sans"; mono = "IBM Plex Mono" for BBLs, dates, numerals). Keep the accent for the domain: replace the link blue with a single civic red #b91c1c for CTAs and focus, keep class C/B/A semantic colours (out #991b1b, watch #92400e, reliable #166534 on white). Hairlines #e0e0e0, canvas #ffffff, canvas-soft #f5f5f5, ink #000000, body #757575 for secondary text.

Aesthetic target: a legal docket printed by a broadsheet: big serif headline for the address, thin black rules between panels, numbered sections (I., II., III.) in the packet, dateline-style timestamps in mono, all-caps small-tracked labels for panel headings, generous white space, zero shadows, zero gradients, square corners.

Files you own: DESIGN.md, CLAUDE.md, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/c/**, src/components/**, README.md (only if a screenshot/description must change), public/**. Do not touch src/lib/** or evals/** or src/app/api/**.

Steps:
1. Rewrite DESIGN.md from the Wired source structure with the tokens above (computed contrast ratios for every text token vs its canvas; write them in). Name: Order-to-Correct-design-system. Delete anything from the old paper/Archivo system.
2. Rewrite the @theme block in src/app/globals.css to the new tokens (keep the same CSS variable NAMES where components use them, e.g. --color-paper becomes white, --color-hair becomes #e0e0e0, so component churn stays small; rename only where the name is now a lie and update usages). Load fonts via next/font/google in layout.tsx.
3. Restyle components: panel headers to small-caps tracked labels with a 1px black top rule; address as the serif hero; Building record numbers in the display serif at 48px; tables with hairline rows; buttons black fill white text square, accent red for the one primary action per view (File packet / Create case); confirm card as a boxed notice with a 2px black border.
4. Verify: `npx tsc --noEmit`; `npx next build`; `uicraft read --as "housing court docket for a tenant and a legal-aid advocate, broadsheet editorial" --dials 2/1/1 --type "Playfair Display" --surface "#ffffff" --accent "#b91c1c" --radius 0 --showpiece none --cwd .`; `uicraft gate --cwd src` exit 0; `npx next dev -p 3111` then `uicraft look --url http://localhost:3111` at 375 and 1440, plus a case page (create one via POST /api/case {bbl:"2043520010",apartment:"4C",firstCondition:{type:"heat",reading:"52F",note:"no heat"}} and open /c/<id>?k=<ownerKey>). Confirm in DOM: body background rgb(255,255,255), h1 font-family contains the serif, no element with box-shadow. Kill the dev server.
5. Commit after each step with `git add <your paths>` (never -A), then `git push origin main`, then `vercel deploy --prod --yes` and confirm https://order-to-correct.vercel.app returns 200 and the new canvas colour in the DOM (bh-multi run qa "goto_url('https://order-to-correct.vercel.app'); wait_for_load(); print(js('getComputedStyle(document.body).backgroundColor'))").

Rules: Next.js 16 (read node_modules/next/dist/docs/ before app-router edits). Use PATH=/Users/kamal/.nvm/versions/node/v24.9.0/bin:$PATH for node commands. Browser only via bh-multi/bhn profiles (qa, deepsurge). No em dashes in any string. No "legal advice" phrase. Semantic tokens only in components, no raw hex. Loading/empty/error states stay. When done, `swarm report` with: tokens chosen, files, the three tells fixed, gate/look output, live URL DOM check. Then stop.
