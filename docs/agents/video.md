You are the VIDEO agent for "Order to Correct" at /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct. Deliver /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/out/order-to-correct-demo.mp4, under 180 s, 1920x1080, one voice, first tool call on camera by 0:12, then upload it to YouTube and record the URL in /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/docs/YOUTUBE.md.

Everything you need exists from the first entry. Copy the pipeline, do not rewrite it:
- Reference pipeline: /Users/kamal/Desktop/devpost/projects/webmcp/video/ (assemble/build_all.sh, build_segment.sh, make_captions.py, gen_pointer.py, cue_filter.py, cdp_record.mjs, timeline.tsv, cues/*.tsv, assets/pointer.png, slides/endcard.png; diagrams/exrender/{build_scenes.py,render.html,serve.py}). Read timeline.tsv and clips/clips.txt first to see the shape of a finished cut.
- Copy assemble/ and diagrams/exrender/ into /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/ (cp -R), then edit paths: build_all.sh OUT name to order-to-correct-demo.mp4; serve.py OUT to /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/diagrams; build_scenes.py palette to THIS repo's DESIGN.md tokens (read DESIGN.md at repo root) and content to THIS product.
- Narration is already generated: /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/narration/line1.wav .. line13.wav from /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/narration.txt (13 lines, male, low pitch, seed 7). Do not regenerate. Measure each with ffprobe for audio_sec.

Beats (map narration lines to visuals; 13 lines):
1 problem.png (52F, eleven mornings). 2 problem.png with the building record numbers called out (102 open class C, 601 days). 3 two-sessions.png (tenant + advocate, two agents). 4 pipeline.png (HPD violations, complaints, 311, registrations -> index with SODA URL beside every number). 5 CLIP tenant window: building_violation_history executed from the page with {}, result in TOOL LOG, Building record panel. 6 CLIP tenant: match_condition_to_code with "no heat at 7am, 52 degrees" then log_condition with the confirm card, human clicks Confirm. 7 CLIP advocate window: tools panel showing 9 tools (assemble_hp_action_packet, request_evidence present; file_packet absent); if possible DevTools Application > WebMCP. 8 CLIP advocate: request_evidence + Confirm, then cut to tenant window showing it arrive with no reload. 9 CLIP advocate: assemble_hp_action_packet + Confirm, packet sections appear. 10 CLIP tenant: file_packet + Confirm, status Filed. 11 CLIP tenant: 311 declarative form, agent-filled fields, human presses Send. 12 two-sessions.png. 13 pipeline.png or a hold on the Building record with the SODA source link; end card.

Diagrams (3 excalidraw scenes rendered to PNG at 1920x1080 via render.html in the deepsurge profile with serve.py on 8099, exactly as the reference did): problem.png (the named human and the gap), pipeline.png (data sources -> derived index -> tools per role), two-sessions.png (owner window vs partner window, tool lists, server gate in the middle). Palette from this repo's DESIGN.md only.

Screen clips: record from the `qa` Brave profile (native WebMCP, CDP port 9318) with `node /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/assemble/cdp_record.mjs --port 9318 --target <url-substring> --out /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/clips/<beat>.mp4 --seconds N --width 1600 --height 1000`, driving the page with `bh-multi run qa "..."` in a second shell while recording. Create a fresh case first via the API (curl -s -X POST https://order-to-correct.vercel.app/api/case -H 'content-type: application/json' -d '{"bbl":"2043520010","apartment":"4C","firstCondition":{"type":"heat","reading":"52F at 7am","note":"eleventh morning without heat"}}' ; tenant = ownerKey, advocate = partnerKey). Drive tool calls from the page itself so the on-page TOOL LOG and confirm cards show: getTools() returns a Promise; executeTool(toolObj, JSON.stringify(args)); click the CONFIRM button. Open DevTools > Application > WebMCP on camera at least once if you can (cmd+option+i then navigate); if the panel cannot be driven, show the on-page "Tools registered in this window" panel instead and say so in the report. Both role windows must appear. The SIMULATED label is not needed here (no simulated data in these products).

Assembly: write /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/assemble/timeline.tsv (same columns), cues/*.tsv for pointer highlights on diagram beats, then `bash /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/video/assemble/build_all.sh`. It must print PASS for duration <= 180 s and no dead-air > 2 s. Run verify_video.sh (called by build_all) and look at the extracted frames list. Fix and rebuild until clean.

Upload: `deepsurge` profile is signed in to YouTube. Follow /Users/kamal/Desktop/devpost/projects/webmcp/docs/YOUTUBE.md exactly (studio.youtube.com, DOM.setFileInputFiles over CDP, title under 100 chars, description with Live and Code links, Public, not for kids). Title: "Order to Correct: a tenant and a legal-aid advocate, each with an agent on one NYC building record (WebMCP)". Description: two sentences from docs/DEVPOST.md, then Live: https://order-to-correct.vercel.app, Code (MIT): https://github.com/kamalbuilds/order-to-correct, "Built for The WebMCP Challenge.", data source line. Wait for processing to finish, open the youtu.be URL in deepsurge and confirm the title renders. Write /Users/kamal/Desktop/devpost/projects/webmcp/order-to-correct/docs/YOUTUBE.md in the same shape as the reference.

Commit as you go: `git add video/assemble video/diagrams video/clips/clips.txt video/out/*.mp4 docs/YOUTUBE.md` (never -A; clips/*.mp4 frames dirs are large, add only the final out mp4 and clips.txt), push origin main at the end.

Rules: PATH=/Users/kamal/.nvm/versions/node/v24.9.0/bin:$PATH. ffmpeg at /opt/homebrew/opt/ffmpeg-full/bin. Browser only via bh-multi profiles qa (recording) and deepsurge (render + YouTube). Never Google Chrome or Playwright. No em dashes. Do not edit product code; if the product blocks a beat, note it and shoot around it. When done, `swarm report` with: final duration, beats built, YouTube URL, what was verified in frames, and any beat you had to shoot around. Then stop.

Shared-resource rule (another video agent runs in parallel on the sibling entry): the `deepsurge` profile is shared. Before any deepsurge work (diagram render or YouTube upload), acquire the lock: `while ! mkdir /tmp/deepsurge.lock 2>/dev/null; do sleep 15; done`, do the work in one contiguous block, then `rmdir /tmp/deepsurge.lock`. Never hold it while recording clips or assembling. Diagram render: use your own serve.py port (order-to-correct 8099, pill-round 8098) and your own /tmp/exrender-<slug> ROOT so the two never write into each other's directories.
